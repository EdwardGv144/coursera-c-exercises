# Sine and Cosine Table

This program prints a table of sine and cosine values for inputs between 0.1 and 1.0 (inclusive), in increments of 0.1.

## 📐 Concepts Used

- Looping with floating-point values
- Calling `sin()` and `cos()` from `<math.h>`
- Formatted output with 4 decimal precision
- Simple table layout using `printf`

```bash
gcc sine_cos_table.c -o table
./table