# File I/O: Average and Maximum of Integer Array

This program reads integers from an input file and:

- Stores them in a dynamically allocated array
- Computes the **average** and **maximum** values
- Outputs the results to both the console and an output file

## 📄 Input Format

The file `numbers.txt` must begin with an integer `N` indicating how many numbers follow. The next `N` lines or space-separated values are the integers.

**Example `numbers.txt`:**
5 22 3 7 8 43

## 📤 Output File

The program writes the results to `answer-hw3.txt` in the same format shown on screen.

## 🛠 How to Compile and Run

```bash
gcc avg_and_max_io.c -o arrayio
./arrayio
